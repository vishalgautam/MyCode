#include<stdio.h>
#include<stdlib.h>
int main(){
    int test=11,i,s,x=1,y=1;
    int alpha[26];
    char *buff,c;
    for(i=0;i<8;i++){
        if(y==6) {
            x++;
            y=1;
        }
        alpha[i]=x*10+y;
        y++;
    }
    alpha[i]=x*10+y;
    alpha[i++]=x*10+y;
    y++;
    for(;i<26;i++){
        alpha[i]=x*10+y;
        y++;
        if(y==6){
            x++;
            y=1;
        }
    }
    for(i=0;i<26;i++)
     printf("%c=%d ",i+'A',alpha[i]);
    printf("\n");   
    scanf("%d",&test);
    getchar();
    while(test--){
        getline(&buff,&s,stdin);
        for(i=0;c=buff[i];i++)
            if(c!=' ')
                printf("%d ",alpha[c-'A']);
        printf("\n");
    }
}
