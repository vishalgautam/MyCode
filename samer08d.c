#include<stdio.h>
#include<string.h>
int mat[1001][1001];
int main(){
    int k,alen,blen,i,j,max;
    char a[1002],b[1002];
    while(1){
        scanf("%d",&k);
        if(!k) break;
        scanf("%s%s",a+1,b+1);
        alen=strlen(a);
        blen=strlen(b);
        for(i=1;i<=alen;i++)
            for(j=1;j<=blen;j++){
                if(a[i]==b[j]){
                    
                
                
                }
            }
    }
}
                
